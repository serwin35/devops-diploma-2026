public_zone: wolffire.dev

# Pliki aplikacji w S3 (Laravel FILESYSTEM_DISK=s3 na produkcji).
# Bucket i tożsamość tworzy moduł usługi wolffire/prod (storage.tf); klucze IAM
# mieszkają w secrets.sops.yml (wolffire_s3_access_key_id / _secret_access_key).
wolffire_s3_bucket: wolffire-app-storage
wolffire_s3_region: eu-north-1

ssh_port: 22022

alert_channel: slack
alert_slack_channel: "#alerts"

bastion_ip: 10.0.110.10
monitoring_ip: 10.0.120.20
cicd_ip: 10.0.120.10
db_ip: 10.0.140.10
k3s_subnet: 10.0.130.0/24

.idea/
.DS_Store
CLAUDE.md

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Ansible
ansible/.vault_pass
ansible/.ansible_priv_key
*.retry

# SOPS - klucz prywatny age nigdy nie trafia do repozytorium.
# Pliki *.sops.yaml sa zaszyfrowane i commitowane celowo.
keys.txt
*.agekey

# Klucze SSH - do repozytorium trafiaja wylacznie *.pub
keys/*_ed25519

# Stan bootstrapu AWS - zawiera klucze IAM
terraform/bootstrap/terraform.tfstate*
